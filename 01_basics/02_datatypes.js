"use strict" // treat all js code as newer version of js


// alert(3+5) // we are using nodejs, not browser

console.log(3 + 
    8);   // this will work but code readability should be high
    
    let name = "Harsh"
    let age = 19
    let isLoggedIn = true
    let state;
    //variables in js

    // number => 2 to power 53(range)
    // bigint => used for nuber greater than number(datatype)
    //  string => ""
    // boolean => true/false
    // null => standalone value(empty value)
    // undefined
    // symbol => unique

    // object

    console.table([typeof undefined, typeof null, typeof "", typeof state, typeof age ]);
